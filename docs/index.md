# Octavian

Octavian is a Python-first astrodynamics and trajectory optimization toolkit
built around readable mission scripts.

The API reference is generated from numpy-style docstrings with mkdocstrings.

## Local docs

```bash
pip install -e ".[dev]"
mkdocs serve
mkdocs build
```
